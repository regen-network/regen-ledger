variables:
  GOPATH: "$CI_PROJECT_DIR/.go"

cache:
  paths:
    - .go/pkg/mod

build:
  image: golang:1.11.5
  script:
    - make install

test:
  image: golang:1.11.5
  script:
    - make test
  artifacts:
    reports:
      junit: "build/TEST_*.xml"
