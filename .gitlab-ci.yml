image: fpco/stack-build:lts-11.6

variables:
  STACK_ROOT: "$CI_PROJECT_DIR/.stack"

cache:
  paths:
    - .stack-work
    - .stack

build:
  stage: build
  script:
     - stack build
     - stack test

# deploy:
#   script:
#     - stack install
#   only:
#     - master
