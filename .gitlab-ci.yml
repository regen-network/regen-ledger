variables:
  GOPATH: "$CI_PROJECT_DIR/.go"

cache:
  paths:
    - .go/pkg/mod

build:
  stage: build
  image: golang:1.11.5
  script:
    - make install

test:
  image: golang:1.11.5
  script:
    - GOFLAGS=-cover make test -coverprofile=coverage.txt -covermode=atomic
  after_script:
    - export CODECOV_TOKEN="ae4050d3-73ab-4ad4-957e-1355539e669f"
    - bash <(curl -s https://codecov.io/bash) -f coverage.txt
  coverage: '/\d+.\d+% of statements/'
  artifacts:
    reports:
      junit: "coverage.txt"

#integration_test:
#  image: golang:1.11.5
#  services:
#    - mdillon/postgis:11
#  script:
#    - GOFLAGS=-cover make test
#  coverage: '/\d+.\d+% of statements/'
#  artifacts:
#    reports:
#      junit: "TEST_*.xml"

#lint:
#  image: golang:1.11.5
#  script:
#    - make lint
